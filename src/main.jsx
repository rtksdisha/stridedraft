import React from "react";
import ReactDOM from "react-dom/client";
import CashflowApp from "./CashflowApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CashflowApp />
  </React.StrictMode>
);
